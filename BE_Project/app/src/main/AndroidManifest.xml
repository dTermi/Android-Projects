<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.be_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <activity android:name=".HelpActivity"></activity>
        <activity
            android:name=".CustomerDetailsActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SelectInvoiceItemsActivity" />
        <activity
            android:name=".EnterExtraDataActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SplashScreen">
            <intent-filter> <!-- Intent Filter determines that the application will start with this activity -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GarmentServicePriceDetailsActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Activity_Shop_Details"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SignInActivity"
            android:windowSoftInputMode="adjustResize"> <!-- Makes so that the FAB moves with the Keyboard -->
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>